import java.util.*;

public class URLify {

    // @param str
    //  String to replace all spaces with %20
    // @param trueLen
    //  The true length of n after the correct repalcements
    // Time Complexity: O(n)
    // Space Complexity: O(n)
    public static void strToUrl(char[] str, int trueLen) {
        if (str == null) return;
        if (str.length == 0) return;

        
    }

    public static void main(String[] args) {
        char[] str = {
            
        };
        int n = 13;

        System.out.println("Input: " + input + " Output: " + output + " length: " + output.length());
    }
}